# 母集団 v3: 開発用の確認データ（凍結した解析器を新しいデータで確かめる）

**抽出・取得・走査の前に書く**（2026-09-24）。書いた後で規則を変えない。変えるなら
`docs/preregistration.md` §5 に逸脱として記録する。決定は `docs/decisions.md` D60。

## 役割（**論文の評価ではない**）

- v3 は**開発用**。凍結した解析器（`analyzer-freeze-1`）が、まだ見ていないデータで壊れていないか・
  誤検出や見落としが無いかを確かめる。見つかった欠陥は直す（D60）。
- **論文の評価は、システムの完成後に学生が別の新しいデータで手動で行う。** その評価データには
  v2（`docs/corpus_sample_v2.json`）と v3（`docs/corpus_sample_v3.json`）の repo を**含めない**。
  開発に使ったデータで評価すると結果が甘くなるため。
- v3 の判定は本記録者 1 人が行う（開発用の確認なので二重判定はしない）。

## 枠と抽出（v2 と同じ枠。v2 の抽出分を除く）

- 枠: `docs/population_v2.md` の列挙（2026-09-20、`evidence/population_v2/enumeration.jsonl`）から、
  v2 と同じ規則で作った母集団（src 側に宣言のある 3,827 repo）。**列挙はやり直さない**（枠を同じにして、
  違いを「解析器が見ていない repo」だけにする）。
- 除外: v2 で抽出した 100 repo（取得後に除いた 13 を含む全部）。
- 抽出: 残りから **seed = 20260924** で **100 件**を無作為抽出（`scripts/sample_population_v2.py
  --exclude docs/corpus_sample_v2.json`）。除外で減った分は補充しない。
- pin: 抽出時に `git ls-remote <url> HEAD` の SHA で固定し `docs/corpus_sample_v3.json` に書く。
  **この pin を取得・走査より先にコミットする。**
- 取得後の条件（v2 の定義 2・3 と同じ）: 宣言ファイルが `mcp` / `fastmcp` を import しないもの、
  宣言ファイルが見つからないもの、取得できないものは件数を出して除く（`scripts/check_population_v2.py`）。

## 走査

- 解析器: `analyzer-freeze-1`。走査の前に `authgap/` の結合 sha256 が `docs/fingerprint.json` と一致する
  ことを確かめ、一致しなければ走らせない。
- `scripts/scan_v2.py --sample <v3 の MCP サーバの標本> --label v3_run1`（Python 3.12、tree budget 180 s、
  深さ 4。v2 と同じ設定）。

## 確かめること（手続きと分母）

- **V1 頑健性**: parse 失敗・tree budget で打ち切った木・例外で落ちた木を全件列挙し、原因を 1 件ずつ書く。
  分母は走査した木の数。
- **V2 CONTRADICTION の精度**: 単位は (木, ユニット, site, kind, 宣言)。**D1 / D2 の矛は全件**判定する
  （60 件を超えたら seed 20260924 で 60 件を抜き取り、抜き取りと明記する）。D3 / D4 の矛は別に
  30 件まで（同じ seed）。判定は原ソースを読み、`docs/contradiction_principles.md` §6 の採った原理で:
  - **正**: その経路は実行時に到達し、その動作は宣言に反する。
  - **誤**: 到達しない / 動作が宣言に反しない / 解析器が読み違えた（原因の分類を書く）。
  - **不明**: 静的に決められない（理由を書く。黙って正・誤に倒さない）。
  分母は判定した件数。正 / 誤 / 不明の件数と、誤の原因の内訳を出す。
- **V3 見落としの抜き取り**: v3 で D1 か D2 を明示し（`D_explicit`）、その宣言に対する矛が 1 件も出て
  いないユニットから **seed 20260924 で 30 件**を抜き取る。ツールの本体と、木の中の呼び出し先を深さ 4
  まで読み、宣言に反する動作があるかを判定する: 解析器が不として出している / **見落とし（誤 clear）** /
  反する動作は無い / 不明。分母は 30（足りなければ全数）。
- **V4 不（静的に決められない）**: 件数と理由の内訳だけを出す（判定しない）。

## 見つかった欠陥の扱い（D60）

誤と見落としは原因ごとにまとめ、直すものは今までと同じ手順（規則 → 反例テスト → 実装 → v2 と v3 の
取り直しと突き合わせ）で直し、`docs/decisions.md` に D61 以降として書く。直さないものは既知の限界として
`docs/open_questions.md` に書く。**学生の最終評価の前に、解析器をもう一度凍結する**（最終凍結）。
