# F0a（真の床）

**支配判定に依存しない測定**（§6 F0a）。ユニットごとの危険効果 kind の
真偽値、構文的 validator 形状、config atom と既定値、annotation の有無、
ゲート述語の存在（支配判定なし）。

再現: `python scripts/f0a.py --sample docs/corpus_sample.json`

**分母は 2 通り出す**（§10）。粗い分母 = 危険効果を持つユニット。
偽陽性クラスを除いた分母 = そこから非 DB の `.execute()` 等だけのユニットを除いたもの。
**§10 の分岐判定は偽陽性クラスを除いた分母で行う**（反証条件 F2）。

## 母集団: app

解析した木 8 本（未取得で繰り上げ 0 本、解析中の例外 0 本、ユニット 0 件 3 本 = **フレームの雑音**）、ユニット 350、危険効果を持つユニット 36（偽陽性クラス除外後 36）

| 指標 | 値 | 関門 | 判定 |
|---|---|---|---|
| `in_tree_resolution_ratio`（**サイト単位**、45 サイト） | 20.0% | ≥ 50% | × |
| 同（行単位、56 行、併記） | 17.9% | — | — |
| validator 保有ツール | 4.3% | ≥ 5% | × |
| opaque 率（主 slot 7 種、**サイト単位**） | 64.4% | ≤ 40% | × |
| opaque 率（主 slot 7 種、行単位、併記） | 62.5% | — | — |
| opaque 率（全 slot、併記） | 66.7% | — | — |
| `remote` 率（**関門ではない**） | 0.0% | — | — |

### D 層別存在率（§10 の D 規則）

| 率 | 偽陽性クラス除外 | 粗い分母 |
|---|---|---|
| `r_kind` | 0.0% | 0.0% |
| `r_dom` | 0.0%（**パーサを書かない**。Def 6 の実装条件） | 0.0% |
| `r_op` | 0.0% | — |
| **`r_D`（和集合）** | **0.0%** | 0.0% |

**分岐: 分岐 3（宣言との差を主張から外し、P0 と D_prev に主張順序を移す）**

**`r_D` は和ではなく和集合である。** `r_kind + r_op` という書き方はしない。
annotation 保有ユニット 0 件のうち、上界を動かす
フィールドを明示しているのは 0 件。読めない形（`D_unknown`）は 0 件。

### 効果と確度

| kind | 件数 |
|---|---|
| DB | 1 |
| EXEC | 3 |
| FS_READ | 8 |
| FS_WRITE | 5 |
| NET | 38 |
| SPAWN | 1 |

確度: {'opaque': 46, 'resolved': 10}

opaque の内訳: {'depth': 16, 'dynamic': 7, 'receiver': 18, 'recursion': 2, 'unresolved': 256}

### validator 形状（§6 F0a の語彙）

{'containment': 2, 'ctor_path': 3, 'exists': 5, 'prefix': 5, 'realpath': 1, 'split': 1, 'urlparse': 2}

ゲート述語を持つユニット 62 / 350

parse 失敗 19 件、cap 到達 5 件、時間上限で未解析のユニット 0 件（**黙って落としていない**）

## 母集団: mcp_server

解析した木 60 本（未取得で繰り上げ 0 本、解析中の例外 0 本、ユニット 0 件 6 本 = **フレームの雑音**）、ユニット 1665、危険効果を持つユニット 284（偽陽性クラス除外後 284）

| 指標 | 値 | 関門 | 判定 |
|---|---|---|---|
| `in_tree_resolution_ratio`（**サイト単位**、255 サイト） | 31.4% | ≥ 50% | × |
| 同（行単位、715 行、併記） | 17.1% | — | — |
| validator 保有ツール | 4.4% | ≥ 5% | × |
| opaque 率（主 slot 7 種、**サイト単位**） | 56.9% | ≤ 40% | × |
| opaque 率（主 slot 7 種、行単位、併記） | 75.6% | — | — |
| opaque 率（全 slot、併記） | 68.3% | — | — |
| `remote` 率（**関門ではない**） | 0.0% | — | — |

### D 層別存在率（§10 の D 規則）

| 率 | 偽陽性クラス除外 | 粗い分母 |
|---|---|---|
| `r_kind` | 0.0% | 0.0% |
| `r_dom` | 0.0%（**パーサを書かない**。Def 6 の実装条件） | 0.0% |
| `r_op` | 0.0% | — |
| **`r_D`（和集合）** | **0.0%** | 0.0% |

**分岐: 分岐 3（宣言との差を主張から外し、P0 と D_prev に主張順序を移す）**

**`r_D` は和ではなく和集合である。** `r_kind + r_op` という書き方はしない。
annotation 保有ユニット 0 件のうち、上界を動かす
フィールドを明示しているのは 0 件。読めない形（`D_unknown`）は 5 件。

### 効果と確度

| kind | 件数 |
|---|---|
| DB | 10 |
| EXEC | 5 |
| FS_READ | 68 |
| FS_WRITE | 141 |
| NET | 460 |
| SPAWN | 31 |

確度: {'opaque': 593, 'resolved': 122}

opaque の内訳: {'depth': 508, 'dynamic': 116, 'receiver': 337, 'recursion': 41, 'unresolved': 2097}

### validator 形状（§6 F0a の語彙）

{'absolute': 1, 'containment': 1, 'ctor_path': 16, 'exists': 24, 'lexical_canon': 2, 'prefix': 13, 'realpath': 8, 'shlex_split': 1, 'split': 34, 'urlparse': 2}

ゲート述語を持つユニット 302 / 1665

parse 失敗 45 件、cap 到達 18 件、時間上限で未解析のユニット 0 件（**黙って落としていない**）

## 母集団: tool_package

解析した木 30 本（未取得で繰り上げ 0 本、解析中の例外 0 本、ユニット 0 件 16 本 = **フレームの雑音**）、ユニット 502、危険効果を持つユニット 82（偽陽性クラス除外後 82）

| 指標 | 値 | 関門 | 判定 |
|---|---|---|---|
| `in_tree_resolution_ratio`（**サイト単位**、97 サイト） | 40.2% | ≥ 50% | × |
| 同（行単位、141 行、併記） | 28.4% | — | — |
| validator 保有ツール | 3.4% | ≥ 5% | × |
| opaque 率（主 slot 7 種、**サイト単位**） | 46.2% | ≤ 40% | × |
| opaque 率（主 slot 7 種、行単位、併記） | 59.1% | — | — |
| opaque 率（全 slot、併記） | 60.6% | — | — |
| `remote` 率（**関門ではない**） | 0.0% | — | — |

### D 層別存在率（§10 の D 規則）

| 率 | 偽陽性クラス除外 | 粗い分母 |
|---|---|---|
| `r_kind` | 0.0% | 0.0% |
| `r_dom` | 0.0%（**パーサを書かない**。Def 6 の実装条件） | 0.0% |
| `r_op` | 0.0% | — |
| **`r_D`（和集合）** | **0.0%** | 0.0% |

**分岐: 分岐 3（宣言との差を主張から外し、P0 と D_prev に主張順序を移す）**

**`r_D` は和ではなく和集合である。** `r_kind + r_op` という書き方はしない。
annotation 保有ユニット 0 件のうち、上界を動かす
フィールドを明示しているのは 0 件。読めない形（`D_unknown`）は 0 件。

### 効果と確度

| kind | 件数 |
|---|---|
| DB | 7 |
| EXEC | 2 |
| FS_READ | 11 |
| FS_WRITE | 11 |
| NET | 91 |
| SPAWN | 19 |

確度: {'opaque': 101, 'resolved': 40}

opaque の内訳: {'depth': 16, 'dynamic': 42, 'receiver': 36, 'recursion': 13, 'unresolved': 461}

### validator 形状（§6 F0a の語彙）

{'absolute': 1, 'ctor_path': 2, 'exists': 3, 'prefix': 7, 'realpath': 1, 'shlex_split': 1, 'split': 7}

ゲート述語を持つユニット 86 / 502

parse 失敗 27 件、cap 到達 2 件、時間上限で未解析のユニット 0 件（**黙って落としていない**）

