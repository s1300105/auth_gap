# AuthGap ブラッシュアップ — 中断時点の状態と再開手順

作成 2026-09-08 17:06（一時停止）。このディレクトリは**新規プロジェクト AuthGap の材料置き場**であり、前身リポジトリ（dispatch-taint-system）の成果ではない。git 未コミット。

---

## 1. どこまで進んだか

| 段階 | 状態 |
|---|---|
| 第 1 版の引き継ぎ資料 | 完了 |
| 4 系統の検証（新規セッション模擬 / 一次資料照合 / 原典との差分 / 審査員） | 完了。blocker 4 件ほか多数を検出 |
| 第 2 版の資料（検証反映済み） | 完了 = `AUTHGAP_BRIEF_v2.md` |
| 7 領域の強化案 | **完了 = `improvements.json`**（13 件。7 領域すべてを覆い、6 領域は 2 版ある） |
| 各案の反証（net-benefit / correctness） | **完了 = `verdicts.json`**（13 件、**全件 amend**。reject は 0） |
| 第 3 版の統合 | **完了 = `AUTHGAP_BRIEF_v3.md`** |
| 第 3 版の新規セッション模擬 | **完了。blocker 5 / major 12 / minor 7 = 24 件を検出** |
| 24 件の反映（v3.1） | **完了。`AUTHGAP_BRIEF_v3.md` に適用済み（冒頭の変更履歴 v3 → v3.1 を参照）。これが現時点の正本** |

## 2. ファイル

| ファイル | 中身 |
|---|---|
| **`AUTHGAP_BRIEF_v3.md`** | **現時点の正本。次のチャットに貼るならこれ**（1210 行。v3.1 = 新規セッション模擬の 24 件を反映済み） |
| `AUTHGAP_BRIEF_v2.md` | 一世代前。差分の確認用にのみ残す |
| `premeasure/` | 前測スクリプト 5 本（591 行）と `PROVENANCE.md`。**元はセッション固有の一時ディレクトリにあり消えうるので救出した。新 repo の `scripts/premeasure/` へ D1 にコピーする** |
| `mutants/` `prototype/` | 検証エージェントが作った mutant fixture と試作の骨格 |
| `improvements.json` | 13 件の強化案。各件に `replacement_text`（そのまま資料に貼れる日本語）、`evidence`（一次資料つき）、`cost_weeks_delta`、`interactions`、`risks` |
| `verdicts.json` | 13 件の反証。各件に `verdict`（全て amend）、`reasons`、`amendments`（適用すべき正確な修正文）、`checked` |
| `brushup_workflow.js` | 生成に使ったワークフロー。統合段の指示に「新規性より精度」の方針が既に書き込んである |
| `RESUME.md` | 本ファイル |

`improvements.json` は領域名がキー。同じ領域に 2 版あるものは、**後から生成された長い方**が一次資料の確認をより多く含む傾向がある。統合時は両方を読んで良い方を採るか併合する。

## 3. 中断中に確定した方針（第 3 版に必ず反映する）

**新規性は主目的ではない。** 先行研究と重なってよく、貢献は「同じ問題をより正確に解く改良手法」でよい。2026-09-08 の利用者の決定。

これに伴う書き換え:

- 先行研究（Python MCP サーバに対する大規模な引数 taint、フレームワーク横断の能力到達と承認ポリシー、記述と実装の不整合測定）は**脅威ではなく比較対象**であり、コーパスや正解データの供給元でもある。
- **主張は「先」ではなく「精度」**。差分は**修正版側**にある。先行研究はサニタイザの**存在**を見るので修正版を clear できない。AuthGap は validator を**等級付け**するので clear できる。両側条件そのものが head-to-head の指標になる。
- §1.5 は新規性の防衛から比較表へ。各先行研究について「走らせられるか」「コーパスを再利用できるか」「どの軸で上回る見込みか」。
- 打ち切り条件の「先行研究が既にやっていたら主張を落とす」は「**追試 + 測定された差分として報告する**」に変える。脆弱側で引き分け、修正版で勝てば成功。
- 正直さは維持。先行研究も修正版を clear できたら、引き分けとして報告する。

## 4. 強化案の主な中身（統合時の要点）

- **CVE 採掘**: 較正対を増補し、両側条件を再定義。`docs/cve_triage.csv` の初版が案の中にある。
- **先行研究**: 一次資料つきで 26 件の差分表と参考文献ブロック。**AuthGap の主張のうち 4 つが既存研究と重なることを特定済み**（上記の方針で扱う）。
- **宣言権限 D**: 2 源から 4 源へ拡張（効果種別 / 承認 / 値域 / 前版）。PyPI の 200 パッケージ 2,941 ツールで**存在率を実測**。合計およそ 12〜15% で、資料の D 規則でいう中間帯。
- **val エンジン**: IR、アルゴリズム、擬似コード、fixture 10 件、週の内訳。
- **支配判定**: CFG 構築規則と 15 mutant を具体的な Python 断片として確定。**うち 11 件は前身の実装を実走させて誤判定を確認済み。**
- **SELECT セル**: 「CVE 対 0」は誤りだった。発生の軸を閉じる修正を持つ対を 3 件特定し、**EXPOSE セルとして再定式化**。「MCP サーバは常に assumed」という一般化も誤りと判明。
- **予算**: §7 の表は実は 47 週で、本当の超過は 13 週。会計単位を「建設週」から「学生週」に変え、34 週へ再構成。

**全 13 件が amend 判定**なので、`replacement_text` をそのまま貼ってはいけない。必ず対応する `verdicts.json` の `amendments` を適用してから使う。

## 5. 再開のしかた

### 方法 A（推奨）: ワークフローを再開して第 3 版を生成する

同一セッション内でのみ有効。

```
Workflow({
  scriptPath: "<session>/workflows/scripts/authgap-brushup-wf_5fa7bae5-494.js",
  resumeFromRunId: "wf_5fa7bae5-494"
})
```

完了済みのエージェントはキャッシュから即座に復元され、統合段と新規セッション模擬だけが走る。統合の指示には §3 の方針が既に書いてある。出力は `AUTHGAP_BRIEF_v3.md`。

**注意**: 途中 2 回、使用上限（429）で検証段が落ちた。上限に余裕がある時間帯に再開すること。

### 方法 B: 別セッションで手作業に統合する

`AUTHGAP_BRIEF_v2.md` を土台に、`improvements.json` の各 `replacement_text` へ `verdicts.json` の `amendments` を適用して差し替える。順序は次を推奨。

1. §1.5（先行研究）を §3 の方針で書き直す。ここが主張全体の前提になる。
2. §0 の修論の一文と null 時の代替主張を、精度の主張に書き換える。
3. §6 の T1（CVE 対）と両側条件。
4. Def 6 と §10 の D 規則（4 源と実測値）。
5. Def 4 と新設の val 設計節。
6. 項目 3 の支配判定設計と 15 mutant。
7. Def 4 / Def 7 / §3 の SELECT → EXPOSE。
8. §7 の週表と §10 の go/no-go を再計算し、**算術を本文に書く**。

各段で、資料が既に持つ訂正（2 つの束、proxy sink、タプル変化の両側条件、規則 W、rubric 1c、F0a/b/c、D 規則、計画項目基準の go/no-go）を壊していないか確認する。

## 6. 未着手のまま残っていること

- 第 0 週の指導教員面談（解析単位の反転、null 時の設問変更の書面受理、拒否分岐）。**実装より先。**
- 第二ラベラーの確保と、責任ある開示プロトコルの文書化。
- 新規リポジトリ `~/Project/research/Master_Project/authgap` はまだ作っていない。

## 7. 後片付けの申し送り

検証エージェントが一次資料を読むために `dispatch-taint-system/tmp_repos/langchain`（約 109 MB、git 未追跡）を clone した。第 1 週の CVE triage で使うなら残してよい。不要なら削除してよい（再取得できる）。判断は利用者に委ねる。
